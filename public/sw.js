/**
 * Pedidos a /sw.js são comuns (extensões, previews). Este site não regista um service worker.
 * Este ficheiro evita que o pedido vá parar ao HTML da SPA e dispare avisos do Vue Router em dev.
 */
void 0
